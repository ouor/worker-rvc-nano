# ==============================================================================
# RVC-Nano Worker Environment Variables
# Copy this file to .env and fill in your values
# ==============================================================================

# ------------------------------------------------------------------------------
# S3 Storage Configuration (Optional)
# If not configured, output audio will be returned as base64 data URL
# Supports: AWS S3, Cloudflare R2, MinIO, DigitalOcean Spaces, etc.
# ------------------------------------------------------------------------------

# S3-compatible endpoint URL
# AWS S3: https://s3.{region}.amazonaws.com
# Cloudflare R2: https://{account_id}.r2.cloudflarestorage.com
# MinIO: http://localhost:9000
S3_ENDPOINT_URL=

# AWS Access Key ID (or equivalent for other S3-compatible services)
S3_ACCESS_KEY=

# AWS Secret Access Key (or equivalent for other S3-compatible services)
S3_SECRET_KEY=

# Bucket name for storing output audio files
S3_BUCKET_NAME=

# AWS Region (optional, default: auto)
# For Cloudflare R2, use "auto"
S3_REGION=auto

# Custom public URL prefix for generated file URLs (optional)
# Use this if you have a CDN or custom domain in front of your bucket
# Example: https://cdn.example.com
S3_PUBLIC_URL=

# ------------------------------------------------------------------------------
# RunPod Configuration (set by RunPod automatically)
# These are provided by RunPod runtime, no need to set manually
# ------------------------------------------------------------------------------

# RUNPOD_POD_ID=
# RUNPOD_API_KEY=

# ------------------------------------------------------------------------------
# Model Paths (Docker container paths, usually no need to change)
# ------------------------------------------------------------------------------

# ASSETS_DIR=/assets
# HUBERT_PATH=/assets/hubert/hubert_base.pt
# RMVPE_PATH=/assets/rmvpe/rmvpe.pt

# ------------------------------------------------------------------------------
# Cache Directories (Docker container paths, usually no need to change)
# ------------------------------------------------------------------------------

# MODEL_CACHE_DIR=/tmp/models
# AUDIO_CACHE_DIR=/tmp/audio
# OUTPUT_DIR=/tmp/output
